const StorageError = @import("errors.zig").StorageError;

/// StorageManager provides the required abstraction for all operations on the storage layer.
/// This can be considered as the entry point to the storage module.
pub const StorageManager = struct {};
